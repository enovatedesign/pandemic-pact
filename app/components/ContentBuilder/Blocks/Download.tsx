import BlockWrapper from "../BlockWrapper";

export default function DownloadBlock({ block }) {
	// const button = block.button;
	// const colour = block.colour;
	// const position = block.position;

	// if (button && colour && position) {
		return (
			<BlockWrapper>
				<p>Download block</p>
			</BlockWrapper>
		);
	// } else {
		// return null;
	// }
}
